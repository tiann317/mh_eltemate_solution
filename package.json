{
  "name": "aegis-notice-monorepo",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "bun --cwd frontend run dev",
    "build": "bun --cwd frontend run build",
    "build:dev": "bun --cwd frontend run build:dev",
    "preview": "bun --cwd frontend run preview",
    "lint": "bun --cwd frontend run lint",
    "test": "bun --cwd frontend run test",
    "backend": "cd backend && uvicorn backend.main:app --host 0.0.0.0 --port 8000 --reload"
  }
}
